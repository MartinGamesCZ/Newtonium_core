{
  "name": "@newtonium/core",
  "module": "src/index.ts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "version": "0.2.1",
  "scripts": {
    "prepare": "bun build --minify --outdir dist src/index.ts && bun build --minify --target=bun --outdir dist src/worker.ts && tsc && cp src/lib dist -r"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.5.5",
    "bun-types": "^1.1.33"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/ws": "^8.5.12",
    "ws": "^8.18.0"
  }
}
